<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UnShelf"
        tools:targetApi="31">
        <activity
            android:name=".product_main"
            android:exported="false" />
        <activity
            android:name=".product_details"
            android:exported="false"
            android:label="@string/title_activity_product_details"
            android:theme="@style/Theme.UnShelf" />
        <activity
            android:name=".Model.authentication.Reset_Pass"
            android:exported="false" />
        <activity
            android:name=".Model.authentication.Verification"
            android:exported="false" />
        <activity
            android:name=".Model.authentication.Reset_Password"
            android:exported="false" />
        <activity
            android:name=".Model.authentication.Seller_Register"
            android:exported="false" />
        <activity
            android:name=".Model.authentication.Customer_Register"
            android:exported="false" />
        <activity
            android:name=".Model.authentication.Customer_Login"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>